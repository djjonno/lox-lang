/*
 * Math Library
 */
class Math {
	static square(n) {
		return n * n;
	}
	static pi {
	    return 3.141592653589793;
	}
}

class Circle {
	init(radius) {
		this.radius = radius;
	}

	area {
		return Math.square(this.radius) * Math.pi;
	}
}

var c = Circle(10);
print c.area;
