/*
 * Math Library
 */
class Math {
	static square(n) {
		return n * n;
	}
}

class Circle {
	init(radius) {
		this.radius = radius;
	}

	area() {
		return Math.square(this.radius) * 3.14;
	}
}

var c = Circle(10);
print c.area();
